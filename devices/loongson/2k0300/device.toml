id = "2k0300"

vendor = "loongson"

arch = "loong_arch64"

name = "Generic LS2K0300 Board"

bsp_packages = [
    "linux-kernel-loongarch-nosimd-6.12.35"
]

initrdless = true

generate_rootfs_tar = true

partition_map = "dos"

num_partitions = 1

[size]
base = 4096
desktop = 4096
server = 4096

[[partition]]
no = 1
type = "linux"
size_in_sectors = 0
mountpoint = "/"
filesystem = "ext4"
usage = "rootfs"
fs_label = "AOSC OS"
